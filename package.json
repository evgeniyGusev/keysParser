{
  "name": "keys-parser",
  "type": "module",
  "description": "Get the value in an object using the string. The string 'person.info.name' will return you 'Pete' from the { person: { info: { name: 'Pete', age: 32 } } }",
  "author": "evgeniyGusev",
  "person": {
    "name": "Evgeniy Gusev",
    "email": "shchelkalinevgeniy@gmail.com",
    "url": "https://github.com/evgeniyGusev"
  },
  "main": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --minify --format=esm --outfile=./build/index.js",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.18.6",
    "c8": "^7.12.0",
    "esbuild": "^0.14.49",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4",
    "vitest": "^0.18.1"
  }
}
